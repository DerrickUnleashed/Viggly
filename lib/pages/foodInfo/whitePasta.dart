import 'package:flutter/material.dart';

class WhitePasta extends StatelessWidget {
  const WhitePasta({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('WHITE PASTA'),
      ),
    );
  }
}