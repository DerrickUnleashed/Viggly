import 'package:flutter/material.dart';

class EggFriedRice extends StatelessWidget {
  const EggFriedRice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text('EGG FRIED RICE'),
      ),
    );
  }
}